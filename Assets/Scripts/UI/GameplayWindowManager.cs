using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Vanaring 
{
    public class GameplayWindowManager : MonoBehaviour
    {
        [SerializeField] private GameObject characterIconWindowManager;
        [SerializeField] private GameObject targetInfoWindowManager;
        [SerializeField] private GameObject buttonIndicatorManager;
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
